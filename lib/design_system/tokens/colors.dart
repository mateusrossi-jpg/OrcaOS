import 'package:flutter/material.dart';

class AferixColors {
  // Primary brand color (Amber Gold)
  static const Color primary = Color(0xFFFFC107);
  static const Color onPrimary = Color(0xFF000000);

  // Secondary accent (Light Blue)
  static const Color secondary = Color(0xFF03A9F4);
  static const Color onSecondary = Color(0xFF000000);

  // Success (Green)
  static const Color success = Color(0xFF66BB6A);
  static const Color onSuccess = Color(0xFF000000);

  // Error (Red)
  static const Color error = Color(0xFFEF5350);
  static const Color onError = Color(0xFFFFFFFF);

  // Warning (Orange)
  static const Color warning = Color(0xFFFFA726);
  static const Color onWarning = Color(0xFF000000);

  // Surface and background for dark premium theme
  static const Color surface = Color(0xFF212121);
  static const Color background = Color(0xFF121212);
  static const Color onSurface = Color(0xFFFFFFFF);
  static const Color onBackground = Color(0xFFFFFFFF);

  // Tertiary color for less prominent UI elements
  static const Color tertiary = Color(0xFF9E9E9E);
  static const Color onTertiary = Color(0xFF000000);
}

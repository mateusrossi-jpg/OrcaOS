name: aferix_flutter
description: Aferix Flutter application
publish_to: 'none' # since this is not intended for publishing
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  equatable: ^2.0.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  test: ^1.24.0

flutter:
  uses-material-design: true

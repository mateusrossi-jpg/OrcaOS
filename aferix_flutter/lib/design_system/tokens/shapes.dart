import 'package:flutter/widgets.dart';
import 'package:aferix_flutter/design_system/tokens/radius.dart';

class AferixShapes {
  static const BorderRadius xs = BorderRadius.all(Radius.circular(AferixRadius.xs));
  static const BorderRadius s = BorderRadius.all(Radius.circular(AferixRadius.s));
  static const BorderRadius m = BorderRadius.all(Radius.circular(AferixRadius.m));
  static const BorderRadius l = BorderRadius.all(Radius.circular(AferixRadius.l));
  static const BorderRadius xl = BorderRadius.all(Radius.circular(AferixRadius.xl));
}

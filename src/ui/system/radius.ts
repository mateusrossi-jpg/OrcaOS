/**
 * AFERIX RADIUS TOKENS
 * Aligned with AFERIX VISUAL PROTOCOL (Phase 4).
 */
export const AferixRadius = {
  xs: 'var(--radius-xs)',    // 8px
  sm: 'var(--radius-sm)',    // 12px
  button: 'var(--radius-md)',// 16px
  card: 'var(--radius-card)',// 24px
  modal: 'var(--radius-modal)', // 32px
  full: 'var(--radius-pill)',// 9999px
};
